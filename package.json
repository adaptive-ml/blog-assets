{
  "name": "blog-assets",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "inline": "bun scripts/generate-inline.ts",
    "index": "bun scripts/generate-index.ts"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
